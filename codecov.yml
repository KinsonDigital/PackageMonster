coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 85%
        base: auto 
        flags: 
          - unit
        paths: 
          - "GotNuget"
      #  # advanced settings
      #   branches: 
      #     - master
      #     - develop
      #     - 
        if_ci_failed: error #success, failure, error, ignore
        informational: false
        only_pulls: false
